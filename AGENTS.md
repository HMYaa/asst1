## Learned User Preferences

## Learned Workspace Facts

- CS149 asst1：根目录 `compile_commands.json`（常用 `bear` 对各 `prog*/` 目录 `make` 合并生成）与 `.vscode/settings.json` 里的 `C_Cpp.default.compileCommands` 配套，供 Cursor/C++ 扩展正确解析并减少标准库头文件误报。
- Gradescope 代码提交文件名：`prob2.cpp`（prog2 的 `main.cpp`）、`prob6.cpp`（prog6 的 `kmeansThread.cpp`）；报告提交 `writeup.pdf`。
- 仓库维护 `readme_cn.md` 作为英文 `README.md` 的中文对照版本。
